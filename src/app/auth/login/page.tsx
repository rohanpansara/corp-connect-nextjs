import LoginForm from '@/components/LoginForm';

const LoginPage = () => {
    return (
        <div className="min-h-screen bg-[#9BC4CB] flex items-center justify-center">
            <LoginForm />
        </div>
    );
};

export default LoginPage;
